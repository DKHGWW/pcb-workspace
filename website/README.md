1. 项目概述
公司名称：四川井明电子有限公司（Sichuan Kinming Electronics Co., Ltd.）

行业：PCB（印制电路板）制造，提供 2-24 层 PCB，应用于航空、通讯、汽车、AI、医疗等领域。

官网类型：企业展示型网站，单页应用风格（前端路由 + 锚点滚动），无需后端数据库。

核心要求：

顶部导航栏带二级下拉菜单。

每个一级页面顶部有自动轮播图（可配置图片、标题、描述，支持中英文）。

页面内容包括公司介绍、行业市场、产品技术、新闻中心、社会责任、联系我们等模块。

底部有“社会责任声明”（绿色制造）以及多列链接导航（与顶部导航结构一致）。

全站支持中英文切换。

公司地址已固定：四川省遂宁市经济技术开发区渠河南路188号电子电路标准厂房一期3号楼。

2. 全局布局与组件
2.1 顶部导航栏（Navbar）
样式：白底，阴影，sticky 固定。

Logo 区：左侧显示中英文公司名称（文字）：

中文：四川井明电子有限公司

英文：SICHUAN KINMING ELECTRONICS CO., LTD

导航菜单项（一级）：共 6 项，均带有下拉二级菜单。

一级菜单	二级菜单项（点击后跳转到对应页面并滚动到锚点）
关于井明	公司简介、制程能力、管理体系、井明荣誉、组织架构、品质保证、井明历史、井明文化
行业市场	网络通信、汽车电子、智能终端、工控医疗、电源、消费电子
产品技术	常规电路板、高密度互联板、高多层电路板、金属基板、制程能力、生产设备、检测设备
新闻中心	公司新闻、行业动态
社会责任	环境保护
联系我们	服务据点、在线留言
语言切换按钮：位于菜单最右侧，显示“English”/“中文”切换，点击后全局文本（导航、页面内容、轮播图文字、底部声明等）中英文切换。

2.2 轮播图（Carousel）
每个一级页面（关于井明、行业市场、产品技术、新闻中心、社会责任、联系我们）顶部都有一个轮播图。

自动轮播（间隔 4 秒），带手动指示点，可点击切换。

轮播图数据结构（示例，实际可硬编码在 JS 中，但需支持后期修改）：

每个页面有独立的一组图片（至少 2-3 张）。

每张图片包含：图片 URL（示例使用 picsum，生产环境可替换）、标题、描述（支持中英文）。

若某页面暂无轮播图数据，则不显示轮播图容器。

2.3 底部区域（Footer）
顺序从上到下：

社会责任声明块：绿色背景圆角卡片，标题“🌿 践行绿色制造，共创可持续未来”，内容为一长段环保承诺文字（中英文）。
多列链接导航：与顶部导航结构一致，共 6 列，每列标题为一级菜单名，下列为对应的二级菜单项。点击链接后，如果不在当前页面则先切换页面，然后滚动到对应子锚点；如果在当前页面则直接滚动。需支持中英文。
页脚版权行：包含公司版权年份、名称，公司地址（中英文），以及社交媒体图标（微信、领英等）。
3. 页面结构与内容
3.1 关于井明页面（About）
顶部轮播图（见 2.2）。

页面内容区域：按二级菜单顺序依次展示以下区块（每个区块 ID 与二级菜单锚点对应）：

公司简介：一段文字描述（详见附录）。

制程能力：表格展示，三列（核心技术项 / 井明标准能力 / 特级能力），行数据见附录。

管理体系：ISO9001, IATF16949, UL 等认证说明。

井明荣誉：列表（国家高新技术企业、四川省专精特新等）。

组织架构：文字描述。

品质保证：文字描述。

井明历史：时间线列表（2011-2013, 2014-2018 等）。

井明文化：文字描述。

3.2 行业市场页面（Markets）
顶部轮播图。

内容区块：按二级菜单顺序展示 6 个行业（网络通信、汽车电子、智能终端、工控医疗、电源、消费电子）。每个区块包含标题和描述文字。

3.3 产品技术页面（Products & Tech）
顶部轮播图。

内容区块：

常规电路板、高密度互联板、高多层电路板、金属基板（各一段描述）。

制程能力：单独表格（项目 / 能力）。

生产设备：图标列表（数字钻孔机、填孔脉冲电镀线等）。

检测设备：图标列表（AOI、飞针测试等）。

3.4 新闻中心页面（News）
顶部轮播图。

内容区块：

公司新闻：列表（至少三条新闻）。

行业动态：列表（至少三条新闻）。

3.5 社会责任页面（CSR）
顶部轮播图。

内容区块：

环境保护：一段文字描述（RoHS、REACH、废水处理、光伏等）。

3.6 联系我们页面（Contact）
顶部轮播图。

内容区块：

服务据点：列表（总部地址、华南办事处、华东办事处）。总部地址精确为：四川省遂宁市经济技术开发区渠河南路188号电子电路标准厂房一期3号楼。

在线留言：表单（姓名、邮箱、留言内容，提交时弹出提示）。

4. 交互行为
点击一级菜单：切换页面内容（隐藏其他页面，显示当前页面），URL hash 更新（如 #about），页面顶部滚动到轮播图。

点击二级菜单（下拉或底部链接）：

如果目标页面不是当前页面 → 先切换到目标页面 → 等待页面渲染完成 → 滚动到该二级菜单对应的子区块（通过锚点 ID）。

如果已在目标页面 → 直接平滑滚动到该子区块。

语言切换：所有文本（导航、轮播图文案、页面内所有静态文本、底部声明、链接标题等）中英文互换。无需刷新页面，使用 JavaScript 动态替换文本内容。

轮播图：自动循环 + 点击指示点切换，切换语言时轮播图文字同步更新（若图片内容依赖语言，则重新渲染轮播图组件）。

5. 多语言数据要求
中英文两套完整文本库，结构一致。

导航标题、各页面所有描述、表格、列表、底部声明、公司地址等均需提供英文版本。

轮播图每页的中英文标题/描述独立存储。

6. 样式与响应式
字体：Inter（Google Fonts）。

主色调：深蓝 (#0f2b5e, #1e4a7a)，强调色 (#2563eb)。

卡片圆角、阴影柔和。

移动端（≤768px）：

导航菜单折叠为汉堡菜单，下拉菜单改为点击展开。

轮播图高度减小。

底部链接区域改为 2 列布局。

7. 部署方式
本项目为「前端单页 (public/index.html) + Node/Express 后端 (server.js)」动态站点，需要 Node.js 运行环境，**不能**直接放到纯静态托管（GitHub Pages / Netlify 静态）上。

推荐部署到云服务器（见下方「部署到国内云服务器」），后台修改的内容会持久化在服务器磁盘的 `config.json`。

8. 附录：静态文本数据（中文摘要）
8.1 公司简介
四川井明电子有限公司拥有标准化生产基地与高精度设备集群，可大批量交付2-24层印制电路板，是PCB产品优质提供商。近十五年发展，产品广泛应用于航空、光模通讯、智能汽车、AI服务器及医疗器械等高端领域。

8.2 制程能力表格（关于井明内）
核心技术项	井明标准能力	特级能力
最大生产层数	24层	30层
最小线宽/线距	75/75μm	60/60μm
最小机械孔径	0.2mm	0.15mm
最大纵横比	20:1	25:1
阻抗控制公差	±10%	±7%
板翘控制	<0.75%	<0.5%
表面处理能力	喷锡、沉金、沉银、OSP、沉锡、选择性沉金+OSP、蓝胶、碳油、喷锡+金手指、电镀金、电镀银、沉镍钯金	–
板材类型	FR-4(普通TG/中TG/高TG)、无卤、金属基板、陶瓷、高频、复合基(CEM系列)	–
8.3 产品技术内的制程能力表（简化）
项目	能力
最小线宽	2.5mil
最小孔径	0.15mm
最大板厚	8.0mm
表面处理	沉金/沉银/OSP/镀金
8.4 底部社会责任声明
井明电子始终将环境保护与社会责任融入企业发展战略。我们严格遵守 RoHS、REACH 等国际环保指令，投资建设先进的废水处理与中水回用系统，厂区光伏发电覆盖率已达30%。同时，我们积极参与社区公益，关爱员工成长，致力于成为负责任、有温度的行业标杆。未来，我们将持续加大环保投入，推动低碳生产，与合作伙伴共同守护绿水青山。

8.5 公司地址（中文）
四川省遂宁市经济技术开发区渠河南路188号电子电路标准厂房一期3号楼

9. 非功能性要求
代码可读性高，注释清晰。

轮播图、多语言切换、锚点滚动等功能应无 bug。

所有二级菜单点击后必须正确跳转或滚动（需处理异步渲染时序）。

移动端菜单点击后自动折叠。

---

## 运行与部署（前后端已连接）

本项目为「前端单页 (public/index.html) + Node/Express 后端 (server.js)」结构，后台可通过 `public/admin.html` 在线修改全站文案与轮播图，数据持久化在根目录 `config.json`。

### 本地运行
```bash
npm install          # 安装 express 等依赖
npm start            # 启动后端，默认端口 4000（与任务管理系统 3000 错开）
# 如需改用其他端口： PORT=3000 npm start
```
启动后访问：
- 官网前台： http://localhost:4000/
- 后台管理： http://localhost:4000/admin.html （默认密码 admin123）

### 前后端连接说明
- 前台加载时通过 `GET /api/config` 拉取站点配置（导航、文案、轮播图），失败时回退到内置默认值。
- 后台登录 `POST /api/login`（密码 admin123）换取 token；保存配置 `POST /api/config` 需在请求头携带 `Authorization: Bearer <token>`，未授权返回 401。
- 配置写入根目录 `config.json`，下次前台加载即生效，实现「后台改 → 前台变」的实时连接。
- 访客「在线留言」经 `POST /api/contact` 提交，服务端校验后落库到根目录 `messages.json`；后台「留言管理」标签页可查看全部提交（需登录）。
- 修改后台密码：启动时设置环境变量 `ADMIN_PASSWORD=你的密码`，或代码内 `server.js` 的 `ADMIN_PASSWORD`。

### 部署到国内云服务器（Ubuntu / Debian，推荐）
适用于腾讯云 / 阿里云「轻量应用服务器」，磁盘持久、国内访问快、可绑定域名与备案。

1. 购买并初始化一台 Ubuntu 20.04/22.04 轻量服务器，SSH 登录。
2. 把整个项目目录上传到服务器（如 `/var/www/pcb-site`）。
3. 在项目目录执行一键部署脚本：
   ```bash
   sudo bash deploy.sh
   ```
   脚本会自动安装 Node.js 18、安装依赖、用 pm2 守护进程并配置开机自启。
4. （可选）用 nginx 做反向代理 + HTTPS：
   - 把 `nginx-pcb.conf` 放到 `/etc/nginx/sites-available/` 并软链启用，把 `server_name` 改成你的域名；
   - 安装 certbot 后执行 `sudo certbot --nginx -d 你的域名` 即可拿到免费 HTTPS 证书。
5. 在云厂商控制台「防火墙 / 安全组」放行 **80、443**（以及临时调试用的 4000）端口。
6. 浏览器访问 `http://你的域名/` 即上线；后台 `http://你的域名/admin.html`。

> 密码安全：上线前请修改 `.env` 里的 `ADMIN_PASSWORD`（或启动时设环境变量），默认 `admin123` 仅用于本地。

### 可选：Docker 部署
若服务器已装 Docker，直接用 `docker compose up -d` 即可（见 `docker-compose.yml`，后台数据通过目录挂载持久化）。

