# 2026-08-26 工作日志

## 网站开发 - 前后台连接
- 搭建了完整的全栈任务管理系统（Node.js + Express + sql.js + 原生前端）
- 项目路径：C:\Users\冷峻王子kris\Documents\pcb
- 后端：Express 托管静态前端 + RESTful API（/api/tasks CRUD、/api/stats、/api/health）
- 数据库：sql.js（纯 JS SQLite）持久化到 backend/app.db
- 注意：better-sqlite3 在该 Windows 环境下原生编译失败，改用 sql.js 方案
- 前端通过 fetch API 调用后端，同源部署无需处理跨域
- 启动方式：cd backend && npm start，访问 http://localhost:3000
- 所有 API 已通过 curl 联调测试（GET/POST/PUT/DELETE/统计/健康检查全部通过）

## 井明电子官网(pcb-site-main) - 上线部署准备
- 项目路径：C:\Users\冷峻王子kris\Desktop\pcb-site-main（四川井明电子动态官网，Node/Express + 静态前端）
- 用户选择「国内云服务器」上线路线（腾讯云/阿里云轻量应用服务器）
- 新增部署文件：.gitignore、.env.example、ecosystem.config.js(pm2)、deploy.sh(Ubuntu一键)、nginx-pcb.conf(反代+HTTPS)、Dockerfile、docker-compose.yml
- server.js 增加 dotenv 支持（.env 读取 ADMIN_PASSWORD/PORT）；package.json 加入 dotenv 依赖
- README 修正「纯静态可部署」错误说法，补充云服务器部署步骤
- 已验证：dotenv 安装、server.js 语法、:5000 冒烟测试 /api/config 与 / 均 200
- 注意：后台改内容写磁盘 config.json，云服务器持久盘可永久保存；npm install 后会更新 package-lock

## 井明官网合并进工作区 + 在线留言落库 + 端口冲突处理
- 用户要求：把 Desktop/pcb-site-main 官网整体合并进 Documents/pcb 工作区统一管理；给在线留言加后端落库；处理 3000 端口冲突。
- 合并结构：官网整体复制到 Documents/pcb/website/（排除 node_modules，后重装依赖）；原任务管理系统(backend/frontend, 端口3000)保留不动。
- 在线留言落库：website/server.js 新增 POST /api/contact（公开，校验 name/email/message 后追加写入 messages.json）、GET /api/contact（需鉴权，供后台查看）；前台 index.html 表单去掉 alert 改为 fetch 提交；后台 admin.html 新增「留言管理」标签页。messages.json 已加入 .gitignore（含用户隐私）。
- 端口冲突处理：官网默认端口由 3000 改为 4000（避开任务管理 3000），同步更新 ecosystem.config.js / nginx-pcb.conf / .env.example / deploy.sh / README 端口引用。
- 新增统一启动脚本：Documents/pcb/start.ps1（Windows，Start-Process 双开 3000+4000）、start.sh（Linux）；根 README 重写为双项目索引。
- 验证：7 项端到端全过（校验/401/登录/落库），3000 与 4000 双监听共存无冲突。
- 注意：Desktop/pcb-site-main 现为冗余副本，可删；当前运行实例为 Documents/pcb/website 的 4000 端口服务。
